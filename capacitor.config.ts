import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicangularlearn.app',
  appName: 'Ionic Angular Learn',
  webDir: 'www/browser'
};

export default config;
